# Explicit search-discovery access for ChatGPT search.
User-agent: OAI-SearchBot
Allow: /

# Search/retrieval access is intentional. Training crawlers are not given a
# separate restriction in this release; the general public crawl rule applies.
# The noindex localization previews remain crawlable so crawlers can read their
# noindex directive while excluding them from search results.
User-agent: *
Allow: /

Sitemap: https://cbt-cards.github.io/sitemap.xml
