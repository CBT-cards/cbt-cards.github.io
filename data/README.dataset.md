# CBT Cards dataset publication

Canonical machine-readable dataset metadata: [`dataset.jsonld`](./dataset.jsonld).

Publication lifecycle and DOI state: [`dataset-publication.json`](./dataset-publication.json).

The current public corpus is a release candidate for persistent archival citation. Until an external archive issues a DOI, the canonical state remains `doi-not-issued`.
